name: testar codin
on:
  push:
    branches:
      - master
jobs:
  build:    
    runs-on: ubuntu-latest  
    steps:                  
      - name: Checkout código
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: executar script com argumentos
        run: python plus.py 3 4

  deploy:
    needs: build 
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout do códin
        uses: actions/checkout@v4
      - name: Isso é um deploy
        run: echo "Enviando para o servidor..."